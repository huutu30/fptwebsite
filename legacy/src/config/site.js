/**
 * 🌐 Site Configuration — Cấu hình domain trung tâm.
 * 
 * Khi đổi tên miền, CHỈ CẦN SỬA FILE NÀY.
 * Tất cả SEO tags (canonical, OG, sitemap) sẽ tự cập nhật.
 * 
 * Sau khi đổi, chạy: npm run build → git push
 */
export const SITE_URL = 'https://fptlapmang.id.vn';
export const SITE_NAME = 'FPT Telecom';
